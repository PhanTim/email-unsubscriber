import {Box, Button} from '@mui/material';

const MarkUnsubscribedButton = ({children, selectedRows}) => {
    return (
        <>
            <Box sx={{p:2, justifyContent:'space-between'}} display='inline-flex'>
                <Button variant='contained' sx={{}} 
                onClick=
                {() => 
                    {

                    }
                }
                >
                    Mark Unsubscribed
                </Button>
            </Box>
        </>
    )
};

export default MarkUnsubscribedButton; 